# FREUID Challenge 2026 — inference dependencies.
# NOTE: torch and paddlepaddle-gpu are installed separately in the Dockerfile
# (they need custom pip indexes). Everything else goes here.

numpy>=2.3,<3
pandas>=2.0
Pillow>=10.0
opencv-contrib-python>=4.8
transformers>=5.10
timm>=1.0.27
scikit-learn>=1.3
mediapipe>=0.10.35
paddleocr>=3.7
tqdm>=4.60
